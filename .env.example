# Store your Gemini / Generative Language API key here for local development
# Do NOT commit your real key to git. Add this file to .gitignore if you copy it.
# Backend environment variables
# Copy to `.env` and fill in real values. Do NOT commit `.env`.

# Required: Google Gemini API key
GEMINI_API_KEY=

# Optional: Port to run the server on
PORT=5050
